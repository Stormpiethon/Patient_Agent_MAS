version: '3.8'

services:
  backend:
    image: ghcr.io/stormpiethon/patient_agent_mas-backend:latest
    container_name: patient-api
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./data:/app/data:ro
    networks:
      - patient-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

  frontend:
    image: ghcr.io/stormpiethon/patient_agent_mas-frontend:latest
    container_name: patient-ui
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - patient-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

networks:
  patient-network:
    driver: bridge

volumes:
  patient_data:
